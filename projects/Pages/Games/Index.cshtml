@page
@model projects.Pages.Games.IndexModel
@{
    ViewData["Title"] = "Game Catalog";
}

<h1>Game Catalog</h1>
<ul>
@foreach (var game in Model.Games)
{
    <li>
        <a asp-page="Stats" asp-route-id="@game.Id">@game.Name</a> (@game.Genre)
    </li>
}
</ul>
